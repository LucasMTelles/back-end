const meumodulo = require('./meu_modulo')
const soma = meumodulo.soma
const subtr = meumodulo.sub

const modulo2 = require('./meu_segundo')
const mult = modulo2.mult
const div = modulo2.div

soma(2,4)
subtr(2,5)
mult(2,4)
div(2,0)
div(3,3)